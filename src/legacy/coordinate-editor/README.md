# Coordinate editor (deprecated)

Se conserva para editar y exportar arrays antiguos. Se accede desde el boton
Coordinate editor (deprecated) y su codigo se carga mediante React.lazy.

Los campos de coordenadas no modifican los paths del SVG. Las zonas activas de la aplicacion
se mantienen en controllers/*.svg con data-button-id. No agregar nuevas funcionalidades aqui.
ButtonEditorForm y ButtonEditorList pertenecen exclusivamente a este modulo.
