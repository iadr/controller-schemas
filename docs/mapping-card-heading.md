# Encabezado de tarjetas de asociaciones

Los elementos interiores de `.mapping-card-heading` se muestran en una fila con `display: flex`, centrados verticalmente y distribuidos a ambos extremos mediante `justify-content: space-between`.

Verificacion estatica con `git diff --check`; sin ejecutar `npm run` ni pruebas dinamicas.
